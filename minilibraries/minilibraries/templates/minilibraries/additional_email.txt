Hello {{book.owner.first_name}},
{{user.get_full_name}} ({{user.get_username}}) has requested your copy of {{book.title|safe}}
This book has already been borrowed/requested by {{borrower}}. Once {{borrower}} returns the book, you can mark it as returned and give it to {{user.first_name}}.
{{user.first_name}}'s email is: {{user.email}}

(This is a bit of a weird system, but bear with me)